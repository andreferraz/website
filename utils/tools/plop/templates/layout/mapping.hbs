import { {{ pascalCase name }}LayoutContentProps } from '@/layouts/{{ kebabCase name }}/{{ pascalCase name }}Layout.types';

/**
 * Maps content from the API to the {{ titleCase name }} page layout
 * @param data JSON data from API
 */
export function format{{ pascalCase name }}PageContent(data: any): {{ pascalCase name }}LayoutContentProps {
	const content: {{ pascalCase name }}LayoutContentProps = {};
	return content;
}
